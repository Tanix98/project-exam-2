import Container from 'react-bootstrap/Container';

function LoadingError() {
    return (
        <Container className='text-center'>
            <h1>Error, could not load content.</h1>
        </Container>
    );
}

export default LoadingError;
